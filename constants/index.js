// @flow strict
const DEFAULT_CONFIG_FILE_PATH = '.localcms.json';

const DEFAULT_CONTENT_FILE_PATH = 'content.json';
const DEFAULT_MEDIA_DIRECTORY = 'contentMedia/';

module.exports = {
  DEFAULT_CONFIG_FILE_PATH,
  DEFAULT_CONTENT_FILE_PATH,
  DEFAULT_MEDIA_DIRECTORY,
};
